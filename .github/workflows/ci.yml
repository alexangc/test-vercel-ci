name: Loader pipeline
on:
  push:
  workflow_dispatch:

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello world"

      - run:
          response=$(curl -s https://test-vercel-ci-beta.vercel.app/api/echo) &&
          echo $response
